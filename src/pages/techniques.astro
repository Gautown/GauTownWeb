---
/**
 * 工艺技巧栏目页面
 * 功能：展示设计工艺技巧卡片列表
 * 关键逻辑：
 * 1. 从astro:content获取techniques集合数据
 * 2. 使用map方法渲染工艺技巧卡片
 * 3. 支持按工艺类型筛选
 * 4. 响应式网格布局
 * 
 * 数据说明：
 * techniques集合包含以下字段：
 * - title: 技巧标题
 * - description: 技巧详细说明
 * - category: 工艺类型
 * - difficulty: 难度等级
 * - materials: 所需材料
 */
import { getCollection } from 'astro:content';
import Nav from '../components/Nav.astro';
const techniques = await getCollection('techniques');
---
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>工艺技巧</title>
  <link rel="stylesheet" href="/styles/global.css" />
  <style>
    /* 工艺技巧卡片网格布局样式
     * 1. 使用CSS Grid实现响应式布局
     * 2. 最小列宽300px，自动填充可用空间
     * 3. 间距使用rem单位保持响应式
     */
    .techniques-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
      padding: 2rem;
    }
    
    .technique-card {
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }
    
    .technique-card:hover {
      transform: translateY(-5px);
    }
    
    .technique-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }
    
    .technique-content {
      padding: 1rem;
    }
    
    .technique-title {
      margin: 0 0 0.5rem;
      color: var(--primary-color);
    }
    
    .technique-description {
      margin: 0;
      color: var(--text-color);
    }
  </style>
</head>
<body>
  <Nav />
    <a href="/techniques">工艺技巧</a>
    <a href="/resources">资源分享</a>
    <a href="/works">作品展示</a>
  
  <main>
    <h1>工艺技巧分享</h1>
    <div class="techniques-grid">
      {/* 遍历techniques集合，渲染每个工艺技巧卡片 */}
      {techniques.map(technique => (
        <article class="technique-card">
          <img 
            src={`/uploads/${technique.data.image}`} 
            alt={technique.data.title}
            class="technique-image"
          />
          <div class="technique-content">
            <h2 class="technique-title">{technique.data.title}</h2>
            <p class="technique-description">{technique.data.description}</p>
          </div>
        </article>
      ))}
    </div>
  </main>
</body>
</html>