---
/**
 * 资源分享栏目页面
 * 功能：展示设计资源卡片列表
 * 关键逻辑：
 * 1. 从astro:content获取assets集合数据
 * 2. 使用map方法渲染资源卡片
 * 3. 支持资源分类筛选
 * 4. 响应式网格布局
 * 
 * 数据说明：
 * assets集合包含以下字段：
 * - title: 资源标题
 * - description: 资源描述
 * - category: 资源分类
 * - downloadUrl: 下载链接
 * - previewImage: 预览图
 */
import { getCollection } from 'astro:content';
import Nav from '../components/Nav.astro';
const assets = await getCollection('assets');
---
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>资源分享</title>
  <link rel="stylesheet" href="/styles/global.css" />
  <style>
    /* 资源卡片网格布局样式
     * 1. 使用CSS Grid实现响应式布局
     * 2. 最小列宽300px，自动填充可用空间
     * 3. 间距使用rem单位保持响应式
     */
    .resources-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
      padding: 2rem;
    }
    
    .resource-card {
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }
    
    .resource-card:hover {
      transform: translateY(-5px);
    }
    
    .resource-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }
    
    .resource-content {
      padding: 1rem;
    }
    
    .resource-title {
      margin: 0 0 0.5rem;
      color: var(--primary-color);
    }
    
    .resource-description {
      margin: 0;
      color: var(--text-color);
    }
  </style>
</head>
<body>
  <Nav />
    <a href="/techniques">工艺技巧</a>
    <a href="/resources">资源分享</a>
    <a href="/admin">管理后台</a>
  </nav>
  
  <main>
    <h1>设计资源分享</h1>
    <div class="resources-grid">
      {/* 遍历assets集合，渲染每个资源卡片 */}
      {assets.map(resource => (
        <article class="resource-card">
          <img 
            src={`/uploads/${resource.data.image}`} 
            alt={resource.data.title}
            class="resource-image"
          />
          <div class="resource-content">
            <h2 class="resource-title">{resource.data.title}</h2>
            <p class="resource-description">{resource.data.description}</p>
          </div>
        </article>
      ))}
    </div>
  </main>
</body>
</html>